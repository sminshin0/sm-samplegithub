apiVersion: v1
kind: ConfigMap
metadata:
  name: go-hello-world-config
  namespace: default
  labels:
    app: go-hello-world
data:
  # 애플리케이션 설정
  PORT: "8080"
  LOG_LEVEL: "info"
  
  # 환경 정보
  ENVIRONMENT: "production"
  VERSION: "1.0.0"